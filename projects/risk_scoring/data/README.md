# Data Layout

This folder keeps data organized and reproducible.

- `raw/` – untouched source files from Kaggle
- `interim/` – cleaned but not feature‑ready
- `processed/` – final feature tables ready for modeling

Raw data is large and should not be committed to git.
